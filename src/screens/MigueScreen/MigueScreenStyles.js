import { makeStyles } from "@material-ui/core/styles";
export const useStyles = makeStyles(theme => ({
    rootContainer:{
        width:'100vw',
        height: '100vh',
        marginTop: '1em'
    }
}));
